export default (theme) => ({
  '@global': {
    body: {
      padding: 0,
      margin: 0,
      outline: 'none',
      fontFamily: 'Roboto',
      background: theme.color.darkGrey,
      color: theme.color.white,
    },
  },
});
